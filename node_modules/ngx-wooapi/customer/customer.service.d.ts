import { HttpClient } from '@angular/common/http';
import { Observable } from 'rxjs';
import { WoocommerceHelperService } from '../helper.service';
import { Customer } from './customer.interface';
export declare class WoocommerceCustomerService {
    private httpClient;
    private wooHelper;
    constructor(httpClient: HttpClient, wooHelper: WoocommerceHelperService);
    /**
     * Create a customer
     * @param customer: Customer
     */
    createCustomers(customer: Customer): Observable<Customer>;
    /**
     * Retrive a customer
     * @param customer: Customer
     */
    retriveCustomers(id: number): Observable<Customer>;
    /**
     * Update a customer
     * @param customer: Customer
     */
    updateCustomers(id: number, customer: Customer): Observable<Customer>;
}
